# dependencies (bun install)
node_modules

# output
out
dist
*.tgz

# code coverage
coverage
*.lcov

# logs
logs
*.log

# run directories: one copied target plus its five stores, per run
runs

# browser-test output: the videos and the traces of one run. The SCREENSHOTS
# are committed (packages/ui/e2e/screenshots) because they are the evidence;
# a video is 4 MB of the same thing and is reviewed by watching it locally.
test-results
playwright-report

# journals
*.sqlite
*.sqlite-shm
*.sqlite-wal

# dotenv environment variable files
.env
.env.*.local
.env.local

# caches
.eslintcache
.cache
*.tsbuildinfo
.tanstack
.vite

# editors
.idea
.vscode

# Finder (macOS) folder config
.DS_Store

# MCP server artifacts
.serena
